<h1>All the Cats!</h1>

  <ul>
    <% @cats.each do |cat| %>
      <li>
        <%= link_to cat.name, cat_url(cat) %>
      </li>
    <% end %>
  </ul>

  <a href="<%= new_cat_url %>">Click to create a cat.</a>
